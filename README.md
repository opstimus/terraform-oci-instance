> ⚠️ **Moved to the monorepo.**
> This module now lives in [`opstimus/terraform-modules`](https://github.com/opstimus/terraform-modules) at `modules/oci-instance`.
>
> ```hcl
> source = "git::https://github.com/opstimus/terraform-modules.git//modules/oci-instance?ref=oci-instance/v1.0.0"
> ```
>
> This repository remains for existing consumers; new development happens in the monorepo.

# terraform-oci-instance
terraform-oci-instance
